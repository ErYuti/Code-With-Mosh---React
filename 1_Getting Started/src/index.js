import React from "react";
//react is module , React is the object that importing from react 
import ReactDOM from 'react-dom';


const element = <h1>Hello world</h1>;
console.log(element);

ReactDOM.render(element, document.getElementById('root'));